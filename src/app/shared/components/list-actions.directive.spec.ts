import { ListActionsDirective } from './list-actions.directive';

describe('ListActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ListActionsDirective();
    expect(directive).toBeTruthy();
  });
});
